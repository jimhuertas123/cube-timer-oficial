import 'package:flutter/material.dart';

class AndroidOllAlgorithmsPage extends StatelessWidget {
  const AndroidOllAlgorithmsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Android OLL Algorithms')),
      body: const Center(child: Text('Android PLL Algorithm Page Content')),
    );
  }
}
