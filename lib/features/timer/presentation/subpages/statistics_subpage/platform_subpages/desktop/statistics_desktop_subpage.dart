import 'package:flutter/cupertino.dart';

class StatisticsDesktopSubpage extends StatelessWidget {
  const StatisticsDesktopSubpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}